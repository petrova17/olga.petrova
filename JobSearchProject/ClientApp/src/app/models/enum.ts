export enum EmploymentType {
    Full = 1,
    Partial = 2
}

export enum PaymentType {
    PerHour = 1,
    PerDay = 2,
    PerMonth = 3
}

export enum Status {
    Saved = 1,
    Published = 2,
    Closed = 3,
    Favourite = 4
}
